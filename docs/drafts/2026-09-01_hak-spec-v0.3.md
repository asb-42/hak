# HAK — API Spec v0.3

> **Status: DRAFT — design-phase synthesis (final fold). Implementation begins on operator go; C1–C10 gate it.**
> Author: pi-203 (seat: pi-203, backend glm-5.3 via tokenrouter) · 2026-09-01 · Review round 3: operator + GLM 5.3
> Supersedes: `2026-09-01_hak-spec-v0.2.md` (and through it the v0.1 draft; the working title "Agent Messaging Bus" / AMB remains in historical filenames and quotes for provenance)
> Inputs folded this round: `2026-09-01_quinn_hak-spec-v02-review.md` (F-1–F-10, verdict: direction right, fold faithful, three findings gate implementation) · `2026-09-01_quinn_audit_claude-comment-hak-v02.md` (Quinn's audit of Claude Sonnet 5's five findings — all valid, one merged into F-1, four net-new; consolidated fold-list of 14 items, no conflicts)
> HAK is a name, not an acronym (intentionally without expansion, per review F-10).

---

## 0. Decision log

### Round v0.2 (kept — provenance and reasoning unchanged)

| # | Change | Provenance |
|---|---|---|
| D1 | **Rename: AMB → HAK.** Service `hak`, database `hak.db`, env vars `HAK_URL` / `HAK_TOKEN`. The old strings survive only in historical filenames and this log. | Operator, 2026-09-01 (resolves Q10) |
| D2 | **Scope claims are append-only events; the lease table is a derived read model (projector).** | Memento §4.1 + acceptance criterion 5; projector pattern per Danwa 2.3 |
| D3 | **`status` structured fields ride in `meta`, not as new envelope fields.** | Memento §4.2 reconciled with Danwa ADR-001 §1/§2 |
| D4 | **Idempotent message emission: `client_msg_id`.** | v0.2 direction 1 |
| D5 | **Cursor discipline is normative (three MUSTs).** | Danwa 2.1, 2.2 |
| D6 | **Admin operations are system envelopes** (`from.seat="hak"`, `meta.kind="admin-op"`). | v0.1 §14 intent, formalized |
| D7 | **ACP syntax: partial adoption, protocol rejected.** | Q12 skim 2026-09-01 |
| D8 | **Danwa adopt-list folded normatively.** | Danwa annotation §2–§3 |
| D9 | **Acceptance criteria are part of the spec** and gate v1. | Memento §6 |

### Round v0.3 (this round — every item from the consolidated fold-list, in Quinn's resolution order)

| # | Change | Provenance |
|---|---|---|
| D10 | **Five server-assigned/derived fields, one normative line:** the server derives `from` from the bearer token and `room` from the path, and assigns `seq`, `id`, `ts`. The POST contract is "envelope minus seq/id/ts/from/room"; client-supplied values for these five fields are rejected with 422. Conformance case C10. | Quinn F-1 + Claude C3, merged (audit item 1) |
| D11 | **`client_msg_id` uniqueness key = `(room, from.seat, client_msg_id)`**, enforced by a unique index. A duplicate within the key returns that seat's original envelope, never another seat's. | Quinn F-2 (audit item 2) |
| D12 | **Dedupe carries a content hash:** the server hashes the canonical JSON of the client-supplied body (everything the client sends — none of the five server-assigned fields). On `client_msg_id` match: identical hash → 200 with the original envelope; **different hash → 409, nothing stored** (Stripe-style idempotency-key semantics — key reuse with different content is an error, never a silent swallow). C6 extended. | Claude C2, audited VALID (audit item 3) |
| D13 | **`meta.kind="response"` is mandatory at emission:** a POST of `task_result` or `review_verdict` without `meta.kind="response"` is rejected 422. The loop guard is only as strong as the marker's mandatoriness. C8 extended. | Quinn F-5 (audit item 4) |
| D14 | **Scope events live in the separate append-only scope log (§3.1), with their own per-room monotonic `scope_seq` (1-based, gapless).** They are **not** envelopes and never appear via `GET /messages` (the type set stays closed). Provenance window: `GET /scopes?history=1` returns past claims. Conflicts are visible in the scope log and in the 409 body (C1). | Quinn F-3 (audit item 6) |
| D15 | **Claim check + insert run in one transaction (`BEGIN IMMEDIATE`), and liveness derives from events — claim + TTL vs. now — never from stored `lapse` markers** (lapse is a compaction-time replay-clarity marker, not the expiry mechanism; a claim expired-but-not-lapsed is immediately reclaimable). Two racing claims yield exactly one 201 and one 409. C1 extended. | Claude C1, audited VALID with sharpening (audit item 5) |
| D16 | **Self-conflict exemption:** a same-seat re-claim of the same resource returns 200 and refreshes the existing claim (TTL renewal without a remembered `scope_id` — the crash-restart-before-TTL case). Only the token holder can do it. C2 extended. | Quinn F-4 (audit item 7) |
| D17 | **Retraction rendering: visible but marked.** The original stays rendered (strikethrough) with an inline link to the retraction envelope; the retraction envelope itself always renders. Hiding the original would contradict the append-only, transparent-history axiom (Q2). New C9. | Claude C4, audited VALID with recommendation (audit item 8) |
| D18 | **Uploads retention (Q18):** recorded default = age-based GC — attachments unreferenced for 30 days are deletable by admin, and every deletion is logged as a system envelope per D6. Message *history* stays unlimited (Q9); this bounds only the uploads dir. Options (a) room-policy inheritance and (c) per-room byte cap recorded as rejected-for-v1. | Claude C5, audited VALID with recommendation (audit item 9) |
| D19 | **Token model: N tokens per seat** (Q11 upheld: per seat, room-independent). Member-revoke kills **all** of the seat's tokens; token-revoke kills exactly one. Both are admin ops and append system envelopes. | Quinn F-8 (audit item 12) |
| D20 | **`reserve` claim kind dropped from v1.** It appeared in the kind enum without a definition; undefined enum values get guessed differently by every client. V2 candidate only if a real case appears. | Quinn F-7 (audit item 11) |
| M | Mechanical fixes: §8 renumbered (items 1–6, then subsections — v0.2 orphaned 5/6 after 8.1–8.3) · C5 split into C5a/C5b (envelope-stream identity vs. projector-state hash — name exactly what each assertion covers) · example hygiene (weight-atlas refs in BDH bodies fixed) · `me` defined as a reserved filter value resolving to the token's seat · HAK acronym note in the header · `hak` reserved-actor line (§5). | Quinn F-6, F-9, F-10 |

Quinn's endorsements are preserved: D2+§3.1, D3, D4, D6, D7, and §15-as-gate stand as endorsed; the §14 no-authority bullet survives verbatim. Not carried (unchanged rejections): Redis Streams, server-side dispatch, nested trees (§12); orchestration stays rejected — the bus never tells a seat what to do next.

---

## 1. Purpose

A small LAN service that gives human and agent seats a **persistent, shared, auditable
message space** per project ("room"), replacing today's implicit channels:

| Today (implicit) | Problem | HAK replacement |
|---|---|---|
| Human relays notes/screenshots between agents | human is a router; format loss (vision OCR) | direct seat-to-seat messages + attachments |
| dark-factory/notes turn files | conventions only; no history query; no presence | typed, queryable envelopes |
| A2A bridge | context deleted after each task; 500s on message/stream | rooms persist; history is the context |
| Backend churn (MiMo retired, GLM in, ...) | communication history dies with the backend | **seats are stable, backends are swappable** |

Design stance: **pull-first** (agent runtimes are turn-based and cannot receive mid-turn
pushes), **Git stays the artifact bus** (HAK is coordination, pointers and history —
not an artifact store), **peer seats over an orchestrator** (memento §2: scopes +
statuses + handovers; no decomposition, no step assignment, no scheduling).

### Non-goals (v1)

- No orchestration/delegation/dispatch — coordination is claims, statuses, handovers.
- No agent-runtime integration plugins, no mid-turn push, no SSE (v2 at the earliest).
- No E2E encryption (LAN trust domain), no federation, no search (v2 candidate: SQLite FTS5).
- No large artifact storage — files by reference (URI + sha256); small attachments only.

---

## 2. Concepts

| Concept | Definition |
|---|---|
| **Room** | Thematic workspace, e.g. `bdh-cl-development`. Has a **charter** (purpose, admin, member list, dispatch table per §8.3; optional `repo_url`/`project` linkage), one message history with total order, optional attachments. Created and administered by the human (v1). |
| **Seat** | Stable identity of a participant, from the IDENTITIES.md register (`operator`, `quinn`, `pi-203`, `pi-50`, `glm-flash`, ...). Seats outlive backends. `hak` is a **reserved system actor, deliberately not in IDENTITIES.md** — it exists only as the `from.seat` of system envelopes (D6). |
| **Backend** | The LLM currently serving a seat, recorded per message (like commit trailers / report headers). Pure metadata — never used for auth. |
| **Token** | Bearer secret per seat, issued by the admin, revocable. The only auth artifact. A seat may hold **N tokens** (D19). |
| **Envelope** | One immutable message (schema §5). The only payload type on the bus. |
| **Cursor** | A room sequence number (`seq`). Clients pull `?since=<seq>`. The server also tracks `last_read_seq` per member (unread counts, "seen" — a UX read model, Q6 split upheld). |
| **Retraction** | The only way to "un-say" something: a new message of `type=retraction` with `reply_to` pointing at the original. History is append-only (provenance culture; see the F5/konfabulation lessons). Rendering: visible but marked (D17). |
| **Scope claim** | A lease on a resource URI (`file://`, `gpu://`, `host://`, `model://`), held by a seat for a TTL, recorded in the **scope log** (D14) as events. Write claims are mandatory for repo/tree paths (charter rule). |
| **Scope log** | The separate append-only log of claim/renew/release/lapse events (§3.1), with its own per-room `scope_seq`. Not the message history; not pullable via `GET /messages`. |
| **Handover** | An `artifact_ref` with `meta.kind="handover"` (optionally `meta.for_seat`), pullable as "open handovers for me" — the typed replacement for luck-based pickup (memento §4.3). |
| **Projector** | A derived read model over an append-only log (live claims, last_read, presence strip, later: FTS5). Projectors can be added/dropped without touching the write path (Danwa 2.3). |

---

## 3. Architecture (proposed, minimal)

- **Stack:** FastAPI + SQLite (WAL mode), single container or systemd unit, ~500–1.000 LOC.
- **Placement:** open — decision §16 Q1.
- **Network:** bind LAN interface only; TLS optional via reverse proxy later.
- **Storage:** one SQLite file (`hak.db`) + one uploads dir. Backup = file copy / litestream (later).
- **Ordering:** single-writer total order per room via SQLite transaction; `seq` per room, 1-based, gapless. The scope log has its own per-room `scope_seq`, same properties (D14).

### 3.1 Event / projector split (D2, D8, D14)

Two append-only logs — messages and scope events — and everything mutable a client
sees is a **projector** over a log, recomputable from its beginning: liveness of a
claim, unread badges, the presence strip, later FTS5. Rule: *nothing that can change
retroactively is stored; it is derived.* This is the same shape Danwa validated in
production and it is what makes the replay guarantee (C5b) cheap.

---

## 4. Auth and join flow

1. Admin issues a token per seat (CLI or endpoint), stores only the hash. A seat may
   hold N tokens (D19); member-revoke kills all of them, token-revoke kills one.
2. Seat calls `POST /v1/rooms/{room}/join` with its token → membership status `pending`.
3. Admin approves via Web UI or `POST /v1/rooms/{room}/members/{seat}/approve` → `member`.
4. Revocation (`revoke`) disables the token(s) immediately (backend-churn / offboarding).

The human is seat `operator` with role `admin` in every room. Room creation is admin-only (v1).
Join confirmation requirement is deliberate: no silent self-enrollment of agents.

---

## 5. Envelope schema

```json
{
  "seq": 4711,                      // server-assigned, per-room monotonic, gapless
  "id": "m_0000004711",             // stable handle for reply_to / links
  "client_msg_id": "pi203-9f3a",    // client-supplied (D4/D11); unique per (room, seat); optional
  "room": "bdh-cl-development",     // server-derived from the path (D10)
  "ts": "2026-09-01T21:40:12.312+02:00",  // server-assigned (D10) — mixed-seat clock skew must not
                                          // bend the audit trail; seq is the order of record
  "from": {"seat": "pi-203", "backend": "glm-5.3"},  // server-derived from the token (D10);
                                                     // backend is the client's free string (Q3)
  "to": {"seat": "pi-50"},          // null → broadcast to the room
  "type": "chat",                   // closed set, §6
  "reply_to": "m_0000004699",       // optional; thread root = first message of chain (Q7)
  "body": "Phase 13 (fi) complete: INIT=ro_last, width 512, step 10000/10000.",
  "attachments": [                  // small files only (default cap 25 MB/file, Q4)
    {"file_id": "f_a1b2c3", "name": "fi_log_tail.txt", "content_type": "text/plain",
     "sha256": "...", "size": 4123}
  ],
  "refs": [                         // artifact pointers — the Git-bus integration
    {"uri": "https://github.com/asb-42/bdh/commit/a945438", "note": "checklist addendum 5"}
  ],
  "meta": {                          // kind-disciplined (D3, D7); context, never conversation content
    "kind": "status",               // "status" | "handover" | "response" | "admin-op" | (room-defined)
    "state": "working_on",          // status: working_on | waiting_on | blocked | done
    "ref": "file:///media/data/coding/bdh",
    "note": "phase 13 (fi), step 10000/10000 — complete",
    "eta": null
  }
}
```

**POST contract (normative, D10):** the client sends the envelope minus
`seq/id/ts/from/room`. The server derives `from` from the bearer token and `room` from
the path, assigns `seq`, `id`, `ts`, and **rejects with 422 any client-supplied value
for these five fields** — provenance is not negotiable on a bus whose core promise is
auditability (C10).

**Idempotent emission (D4, D11, D12):** `client_msg_id` is optional but, when present,
unique per `(room, from.seat, client_msg_id)` (unique index). The server stores a
content hash over the canonical JSON of the client-supplied body. On a key match:
identical hash → 200 with the original envelope; different hash → 409, nothing stored.
A retried POST never duplicates an envelope; a reused key with new content never
silently swallows it.

**`meta` discipline (normative):** every `meta` object carries exactly one `kind`.
`meta` is additional data — never conversation content. Recognized kinds:

| kind | Carried by | Fields |
|---|---|---|
| `status` | `type=status` | `state` ∈ {working_on, waiting_on, blocked, done}, `ref`, `note`, optional `eta` |
| `handover` | `type=artifact_ref` | optional `for_seat` (directed handover); makes "open handovers for me" a single filtered pull |
| `response` | `type=task_result`, `type=review_verdict` | marker only — **mandatory at emission** (D13: POST without it → 422); clients MUST NOT auto-respond (§8.2) |
| `admin-op` | system envelopes (`from.seat="hak"`) | `op` ∈ {member_approve, member_revoke, token_issue, token_revoke, token_revoke_all, room_create, attachment_delete}, `target` |

**System envelopes (D6):** every admin-privileged operation appends an envelope with
`from.seat="hak"`, human-readable `body`, and `meta.kind="admin-op"`. The room audit
trail is therefore replayable like everything else — no side channel.

---

## 6. Type vocabulary (v1, closed set)

| type | meaning |
|---|---|
| `chat` | ordinary conversation |
| `status` | heartbeat/status; structured fields in `meta.kind="status"` |
| `task_request` | request for work or status from another seat (expects `task_result`) |
| `task_result` | answer to a `task_request` (threaded via reply_to; `meta.kind="response"` mandatory, D13) |
| `artifact_ref` | pointer to a commit/report/file (+ sha256); `meta.kind="handover"` + optional `meta.for_seat` marks a handover |
| `review_verdict` | structured review outcome (pass/fail + findings pointer; `meta.kind="response"` mandatory, D13) |
| `retraction` | corrects/retracts a prior message (reply_to mandatory) |

The set stays deliberately small. Everything structured-but-unlisted goes through `chat`
plus a room-defined `meta.kind` until the design phase promotes it (fat-event rule:
new capability = new metadata, never a new type). Scope events are **not** envelope
types — they live in the scope log (D14).

---

## 7. Endpoints (v1)

All require `Authorization: Bearer <token>`. JSON in/out.

### Rooms & membership

```
GET  /v1/health
GET  /v1/rooms    # rooms the token can see
POST /v1/rooms    # admin: {name, charter}
GET  /v1/rooms/{room}    # charter, members, admin
POST /v1/rooms/{room}/join    # → pending
POST /v1/rooms/{room}/members/{seat}/approve     # admin
POST /v1/rooms/{room}/members/{seat}/revoke      # admin (kills ALL the seat's tokens, D19)
GET  /v1/rooms/{room}/members    # incl. last_poll (soft presence), last_read_seq
```

### Messages — push & pull

```
POST /v1/rooms/{room}/messages   # envelope minus seq/id/ts/from/room (D10)
                                 # client_msg_id honored for dedupe (D11/D12):
                                 #   new key            → 201 with the stored envelope
                                 #   key match, content → 200 with the original envelope
                                 #   key match, changed → 409, nothing stored
                                 #   task_result/review_verdict without meta.kind="response" → 422 (D13)
GET  /v1/rooms/{room}/messages   # THE workhorse (pull):
     ?since=<seq> &until=<seq> &from=<seat> &to=<seat|null>
     &type=<t> &thread=<msg_id> &meta_kind=<kind> &for_seat=<seat|me>
     &limit=100 (max 500) &order=asc|desc
GET  /v1/rooms/{room}/messages/{id}
POST /v1/rooms/{room}/read    # {"seq": 4711} → set member's last_read_seq
```

Pull semantics: `since` returns messages with `seq > since` in ascending order — a cursor
that survives server restarts, client restarts, and gaps. An agent that polls at every
seam never misses anything and never double-processes (idempotent by seq). Filters:
`meta_kind` (e.g. `handover`) and `for_seat` (reserved value `me` resolves to the
token's seat) make "open handovers for me" one query.

### Scope claims (D2, D14, D15, D16)

```
POST   /v1/rooms/{room}/scopes            # {resource_uri, kind, note?, ttl_min?}
                                          #   new claim           → 201 {scope_id, scope_seq, expires_at}
                                          #   same seat, same resource → 200, refreshes the claim (D16)
                                          #   conflict → 409 {conflicting: {holder, kind, expires_at}}
                                          #   — never silently queued
POST   /v1/rooms/{room}/scopes/{id}/renew # extends TTL → 200 {expires_at}
DELETE /v1/rooms/{room}/scopes/{id}       # release (own claim or admin) → 204
GET    /v1/rooms/{room}/scopes?active=1   # live claims (projector view; presence strip reads this)
GET    /v1/rooms/{room}/scopes?history=1  # past claims — the provenance window (D14)
```

- `kind`: `write` | `read-exclusive` | `exclusive` | `share[n]` (memento §4.1; `reserve`
  dropped from v1 — D20, v2 candidate only if a real case appears).
- **Atomicity (D15, normative):** the conflict check against live-claim state and the
  claim-event insert run in **one transaction (`BEGIN IMMEDIATE`)**. Liveness is
  derived from events — claim + TTL vs. now — and never waits for stored `lapse`
  markers (lapse is a compaction-time replay-clarity marker, not the expiry
  mechanism). A claim expired by TTL but not yet lapsed is immediately reclaimable.
  Two racing claims yield exactly one 201 and one 409 (C1).
- Conflict rule at claim time: exclusive vs. any → 409; share kinds advertise capacity
  (charter policy, not hardcoded). Same-seat re-claim of the same resource is the one
  exemption: 200 + refresh (D16) — the crash-restart-before-TTL case, safe because
  only the token holder can do it.
- Claim, renew, release are **events** in the scope log; `expires_at` is derived from
  the last event + TTL. A crashed seat cannot deadlock a resource: the projector stops
  presenting the claim after TTL without any write.
- Convention (charter rule, one line): **write claims are mandatory for repo/tree
  paths**; read seats never claim and are unlimited.

### Files

```
POST /v1/files        # multipart upload, size-capped → {file_id, sha256, size, name, content_type}
GET  /v1/files/{id}   # download (content-type preserved)
```

Large artifacts never go through here: use `refs` with URI + sha256 and keep the payload
in Git / /srv/coding. Uploads are for the screenshot-class of things the human had to
relay by hand. **Retention (D18/Q18):** recorded default — attachments unreferenced for
30 days are deletable by admin, each deletion logged as a system envelope; message
history stays unlimited regardless. This bounds the uploads dir on a shared-disk host.

### Admin/ops

```
POST /v1/tokens               # admin: issue token for a seat (N per seat allowed, D19)
POST /v1/tokens/{token_id}/revoke   # kills exactly one token (D19)
```

Every admin/ops call appends its system envelope (D6) before returning.

---

## 8. Delivery semantics

1. **Total order** per room (seq, gapless, single writer). Timestamps are informational;
   `seq` is the order of record (D10).
2. **Broadcast** = visible to all room members (membership-based, not presence-based).
   Presence (last_poll) is cosmetic in v1.
3. **Direct messages** are recorded in the same room history with a `to` field and are
   **readable by all members** — transparent bus, no hidden channels (Q2 proposal).
   If a truly private channel is ever needed, spin up a separate room.
4. **At-least-once processing is the client's job via cursors**; emission is
   exactly-once via `client_msg_id` + content hash (D4/D11/D12). Store your last
   processed seq, poll `since`, process, advance. Server-side `last_read_seq` is for
   unread/seen UX only (Q6 split).
5. **Immutability:** no edits, no deletes. Corrections are `retraction` messages;
   rendering is visible-but-marked (D17).
6. **No mid-turn push in v1.** Agents poll at seams (turn start/end, tool-call
   boundaries). SSE/long-poll is v2 and only ever an optimization of the same cursor
   semantics.

### 8.1 Cursor discipline (normative — D5)

- **Record your cursor *before* issuing a `task_request`** (subscribe-before-trigger,
  Danwa 2.1): closes the gap between "request sent" and "next poll".
- **Process the batch fully, then persist the cursor.** A crash mid-batch replays from
  the last processed seq (write-ahead discipline; at-least-once processing with
  idempotent handling by seq).

### 8.2 Loop guard (normative — D5, D13, Danwa 2.2)

Messages carrying `meta.kind="response"` (`task_result`, `review_verdict`) MUST NOT
auto-trigger a further response. The marker is **mandatory at emission** (server
rejects unmarked response types with 422, D13) — the guard is exactly as strong as
the marker is reliable. A client may surface response-class messages and act on them
when its user/operator asks; reflex-like polling logic must skip them.

### 8.3 Dispatch table (charter field — Danwa 2.4)

Each room charter records who consumes what. v1 is deliberately trivial:

| seat class | consumes | emits |
|---|---|---|
| human (`operator`) | reads all | `chat`, admin ops |
| agent seats | pull all types | `chat`, `status`, `task_request`, `task_result`, `artifact_ref`, `review_verdict`, `retraction` |

Per-seat `subscriptions` is a v2 candidate. Writing the trivial table down now
prevents implicit conventions later.

---

## 9. Human Web UI (requirements, not implementation)

- Chat-like view: room list with unread badges, message stream, thread view (reply_to),
- Composer: text, type selector, attach small file, reply/broadcast toggle.
- Admin panel: pending join requests → approve/revoke, token issue/revoke (per-token
  and per-seat), charter display.
- Filters: by seat, type, thread, `meta.kind`. Attachment previews for images (the screenshot case).
- Read state: marks last_read automatically; shows per-member last_poll and last_read.
- **Retraction rendering (D17):** a retracted message stays visible, struck through,
  with an inline link to its retraction envelope; the retraction envelope itself
  always renders. Never hidden — hiding would contradict the transparent-history axiom.
- **Presence strip (memento §4.2):** one line per seat from the latest
  `meta.kind="status"` envelope — current state + ref — beside the live scope claims
  (`GET /scopes?active=1`).

---

## 10. Agent client guidance

- Token in env var (`HAK_TOKEN`), base URL in (`HAK_URL`, e.g. `http://gx10:8890`).
- Poll at **seams**: turn start (catch up: `GET messages?since=<cursor>`), after finishing
  a work item (`status` or `task_result`), before ending the turn.
- Prefer filtered pulls (`type=status`, `meta_kind=handover&for_seat=me`) over full-history reads.
- Record `backend` in every message (convention from IDENTITIES.md — same as commit trailers).
- Honor §8.1/§8.2 cursor discipline and loop guard; send `meta.kind="response"` on
  every `task_result`/`review_verdict` or the server rejects the POST.
- Never send `from`, `room`, `seq`, `id`, `ts` — the server owns all five (D10).
- **Claim before you write** (charter rule): `POST /scopes` with `resource_uri=file://...`
  before touching a repo/tree; renew as part of the working loop; release when done.
- Minimal client is five calls (memento §5): `claim(resource)`, `renew()`, `release()`,
  `status(state, ref)`, `pull(since)` — invoked at seams.

```bash
curl -s "$HAK_URL/v1/rooms/bdh-cl-development/messages?since=$CURSOR&limit=200" \
     -H "Authorization: Bearer $HAK_TOKEN"
curl -s "$HAK_URL/v1/rooms/bdh-cl-development/messages" \
     -H "Authorization: Bearer $HAK_TOKEN" -H "Content-Type: application/json" \
     -d '{"type":"status","client_msg_id":"pi203-0042","to":null,"body":"phase 15 (bg) step 4200/10000, on schedule","meta":{"kind":"status","state":"working_on","ref":"file:///media/data/coding/bdh","eta":"2026-09-01T23:00:00+02:00"}}'
```

---

## 11. Relationship to existing channels (explicit, so nothing is double-tracked)

| Channel | Stays? | Role |
|---|---|---|
| **Git (asb-42/bdh)** | yes, unchanged | artifact bus: code, reports, reviews, checklist — HAK carries `artifact_ref` pointers + sha256, never the artifacts |
| dark-factory/notes | absorbed over time | turn-file content becomes envelopes; directory stays as archive until then |
| A2A (FastA2A bridges) | yes, for live dialogue | stateful interactive sessions; HAK is the durable/history layer A2A lacks |
| GrokBot | unchanged | orchestrator topology; HAK is peer coordination — complementary, not competing |
| IDENTITIES.md | yes | seat register; HAK tokens follow it. `hak` is reserved and deliberately not in it (D6). |

---

## 12. Alternatives considered

| Option | Verdict | Why |
|---|---|---|
| Matrix/Synapse | rejected for v1 | robust rooms/history/files/bots, but heavy ops (Postgres, federation we do not need), bots are second-class, charter/join-approval custom anyway |
| Mattermost / Zulip | rejected | same class: generic chat weight, no seat/backend model, licensing/ops overhead |
| **ACP** | **skimmed (Q12, resolved)** | HTTP-native agent messaging — closest protocol analog. Verified 2026-09-01: run-oriented (runs/events/sessions), no persistent rooms, no history cursors, no threading, no charter/join model, no human chat UI. **Syntax partially adopted (D7): `content_type` on attachments; kind-disciplined `meta`.** Protocol not implemented. ACP has since joined A2A under the Linux Foundation. |
| **AgentRoom** (MCP server) | watch | agents join real-time chat rooms with humans — nearest open-source concept, but MCP-scoped, not a room-history bus |
| A2A | already in use, limited | task-delegation protocol; our live finding: context deleted per task, 500s — not a history layer |
| Redis Streams (Danwa's bus) | rejected | durable fan-out at throughput we do not have; the in-memory fallback loses replay — a dual-path cost. SQLite-only: single path, durable by default (Danwa 3.1) |
| Server-side workers/dispatch | rejected | would quietly reintroduce the orchestrator the memento rejected; dispatch stays client-side at seams (Danwa 3.2) |
| Nested event trees | rejected | right for debates, wrong for a bus; threads stay flat reply_to chains (Danwa 3.3, Q7 proposal) |
| Build small (chosen) | — | exact schema (seats, backends, charter, cursors, claims, provenance types) matters more than features; FastAPI+SQLite keeps it auditable in one file |

---

## 13. Scope

**v1 (implementation candidate; operator go pending):** rooms + charter (incl. dispatch
table, repo linkage), join/approve/revoke, N tokens per seat (D19), envelopes (7 types +
meta kinds, response-kind mandatory at emission), broadcast + direct (transparent),
idempotent emission (`client_msg_id` + content hash, D11/D12), server-derived
provenance fields (D10), cursor pulls + normative cursor discipline, read cursors,
scope claims (separate event log + projector, atomic claim, self-reclaim, provenance
window), status envelopes, handover refs, retraction with visible-but-marked rendering
(D17), small attachments with age-GC retention (D18), artifact refs, system envelopes
for admin ops, human Web UI (incl. presence strip, retraction rendering), agent API,
conformance script (§15, C1–C10). No push, no SSE, no search, no server-side dispatch,
no `reserve` claims (D20).

**v2 candidates:** SSE/long-poll, presence beyond last_poll, per-seat `subscriptions`,
typed webhooks, FTS5 search (as projector), per-room retention policy, cross-room
scopes (only if a real case appears), `@-mention` wake (Q13), `reserve` claim kind
(D20, only if a case appears).

---

## 14. Security notes

- **Provenance is server-owned (D10):** `from` derives from the token, `room` from the
  path, and `seq/id/ts` are server-assigned; client-supplied values for these five
  fields are rejected 422. No seat can speak as another seat; mixed-seat clock skew
  cannot bend the audit trail.
- Tokens: 32+ random bytes, shown once, stored hashed; revocation is immediate
  (per-token or per-seat, D19).
- LAN-only bind; optional TLS via reverse proxy (v2); no public exposure, ever.
- Uploads: served only from the uploads dir with generated names; no path traversal;
  size cap enforced before accept; MIME not trusted for execution. `content_type` is
  descriptive (D7), never an execution hint. Age-GC deletion is admin-only and logged
  as a system envelope (D18).
- The message history is the audit trail — append-only by design; admin-privileged
  operations are system envelopes in the room they affect (D6), not log lines elsewhere.
- Scope claims carry no authority: they are coordination signals, not locks. A claim
  does not (and cannot) enforce anything at the filesystem level; enforcement remains
  seat discipline + review, exactly as today — the claim makes the discipline *visible*.

---

## 15. Acceptance criteria (normative — gate v1; the conformance script ships with the implementation and must pass C1–C10)

- **C1 Scope conflict (racing):** two concurrent exclusive claims on one resource →
  exactly one 201 and one 409 (single-transaction check+insert, `BEGIN IMMEDIATE`);
  the 409 carries holder + expiry; both events visible in the scope log. A claim
  expired by TTL but not yet lapsed is immediately reclaimable (liveness derives
  from events + TTL, never from lapse markers).
- **C2 Crash safety + self-reclaim:** a claim without renewal expires via the
  projector ≤ TTL after process death; the resource is reclaimable without admin
  action. A same-seat re-claim of its own live claim returns 200 and refreshes the TTL.
- **C3 Handover determinism:** after an `artifact_ref` with `meta.kind="handover"`
  lands, the named seat's next seam-pull returns it (send, pull with fresh cursor,
  assert presence and order).
- **C4 Presence strip:** live states for ≥ 2 concurrent seats; a `done` status clears the line.
- **C5a Replay determinism (envelopes):** byte-identical re-serialization of the full
  envelope stream of a room from seq 0.
- **C5b Replay determinism (state):** projector state hash equal before and after
  rebuild from the logs alone (messages + scope events) — the derived view, not the
  log bytes, is what must reconstruct.
- **C6 Idempotent emission:** the same `client_msg_id` POSTed twice with identical
  body → one envelope, two 2xx responses naming the same `id` (uniqueness key: room,
  seat, client_msg_id). The same key with a different body → 409, nothing stored.
- **C7 Cursor discipline:** a client crash mid-batch replays the full batch from the
  last persisted cursor; nothing between cursors is lost (replay over a kill -9 of the
  reference client).
- **C8 Loop guard, both halves:** (a) a `task_result` addressed to a reflex-polling
  reference client never produces a second automated response; (b) an unmarked
  `task_result`/`review_verdict` (no `meta.kind="response"`) is rejected 422 at POST.
- **C9 Retraction rendering:** a retracted message remains rendered with visible
  retraction marking and a link; the retraction envelope itself always renders.
- **C10 Server-owned provenance:** a POST carrying client-supplied
  `from`/`room`/`seq`/`id`/`ts` is rejected 422 and nothing is stored; a forged
  `from.seat` never reaches the log; the stored envelope shows the server-derived
  values.

---

## 16. Open design questions (standing — recorded defaults pending operator go)

Resolved to date: **Q10** (name → HAK, D1) · **Q12** (ACP skim, partial syntax
adoption, D7). This round resolved no further Q-numbers but set recorded defaults via
D16–D20. Everything below carries a standing proposal; the operator's go message
either confirms the defaults or lists overrides — that message closes the design phase.

| # | Question | Standing proposal / recorded default |
|---|---|---|
| Q1 | Host placement? | GX10 (already hosts pi-50; 820 GB free; systemd) |
| Q2 | Direct-message visibility? | fully transparent (all members read `to`-messages); privacy = separate room |
| Q3 | `backend` field: free string vs registry? | free string, convention from IDENTITIES.md (as with commit trailers) |
| Q4 | Attachment cap? | 25 MB/file, room-configurable later |
| Q5 | Room creation? | admin-only in v1 |
| Q6 | Cursor storage split? | client-side cursor (source of truth for processing) + server-side last_read (UX only) |
| Q7 | Thread semantics depth? | flat reply_to chains; thread root = walk to first ancestor — no nested trees |
| Q8 | Message body cap? | 64 KB; longer content = attachment or ref |
| Q9 | Retention (messages)? | unlimited in v1 (scale is tiny); revisit if history > 100k messages |
| Q11 | Tokens per seat? | per seat, room-independent; **N tokens allowed** (D19) |
| Q13 | `@-mention` to wake a human? | v2; for v1 the human reads the room |
| Q14 | `task_request` deadline/priority field? | resist scope creep — v2 if it hurts (would ride in `meta` per the fat-event rule) |
| Q15 | Default claim TTL? | 30 min; share-capacity policy per resource class is charter-level |
| Q16 | Does `blocked` status auto-notify the admin? | notify vs. badge — operator preference |
| Q17 | Cross-room scopes? | v2, only if a real case appears |
| Q18 | Uploads retention? | **recorded default (D18):** age-GC — unreferenced after 30 days, admin-deletable, every deletion a logged system envelope; alternatives (room-policy inheritance, per-room byte cap) rejected for v1 |

---

*This document is the design-phase synthesis: it folds Quinn's v0.2 review (F-1–F-10)
and Quinn's audit of Claude Sonnet 5's comment (four net-new + one merged into F-1)
— the consolidated 14-item fold-list, resolved as D10–D20 plus mechanical fixes, every
item traceable in §0. Per the review seat's verdict, with the gating findings folded
the spec is implementation-ready; C1–C10 hold it to that. The operator's go on the
recorded defaults (§16) closes the design phase; the pi-side bridge extension
(five-call wrapper on seam events) and the service implementation start together
after that go. Feedback as "Q# → answer/objection" or free-form.*
